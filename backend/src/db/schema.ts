// Central re-export barrel for all Drizzle table definitions.
// When you add a new feature's schema file, export it from here.
// drizzle.config.ts and src/config/db.ts both point at this file.
//
// Uncomment / add each line as the corresponding plan is executed:
//   Plan 02 (auth):    export * from "#features/auth/auth.schema.js";
//   Plan 03 (events):  export * from "#features/events/events.schema.js";
//                      export * from "#features/events/ticket-tiers.schema.js";
//   Plan 04 (orders):  export * from "#features/orders/orders.schema.js";
//                      export * from "#features/tickets/tickets.schema.js";

// Placeholder — remove once the first feature schema is added.
export {};
